import React from 'react';
import { useParams } from 'react-router';

const JobDetails = () => {
    const { id } = useParams()
    
    
    return (
        <div>
        
        </div>
    );
};

export default JobDetails;